# A minimal config so `supabase` CLI users can link this folder.
# Dashboard SQL apply works without the CLI.

project_id = "haaland-vs-mbappe"

[api]
enabled = true

[db]
major_version = 15

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]
